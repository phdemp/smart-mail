---
phase: 03-user-correction-loop
plan: "03"
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/classifier.js
autonomous: true
requirements:
  - CORRECT-05

must_haves:
  truths:
    - "classifyEmail() checks sender_rules BEFORE rulesClassify() (Tier 0 fires before Tier 1)"
    - "If a sender_rules row exists for (user_id, domain), classification stores source='rule' and returns immediately"
    - "The sender_rules lookup query includes both user_id and domain in WHERE clause"
    - "classifyEmail() still reaches Tier 1 regex when no sender rule matches"
  artifacts:
    - path: "src/classifier.js"
      provides: "Tier 0 sender-rule override in classifyEmail()"
      contains: "SELECT category FROM sender_rules WHERE user_id = ? AND domain = ?"
  key_links:
    - from: "src/classifier.js classifyEmail()"
      to: "sender_rules table"
      via: "db.prepare().get(userId, domain)"
      pattern: "sender_rules WHERE user_id"
---

<objective>
Insert a Tier 0 sender-rule lookup into `src/classifier.js` at the top of `classifyEmail()` —
before the Tier 1 regex rules tier. When a sender rule exists for the email's domain, short-circuit
immediately to `storeClassification()` with `source: 'rule'` without touching the LLM.

Purpose: This is the "downstream consumer" requirement that makes the correction loop meaningful.
Corrections stored via the /reclassify endpoint must actually change future classification behavior.
Without this, the feedback loop is closed on paper but not in practice.

Output: `src/classifier.js` with a Tier 0 block that mirrors the shape of the existing Tier 1
short-circuit pattern.
</objective>

<execution_context>
@C:/Users/Sudeep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sudeep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-user-correction-loop/03-CONTEXT.md
@.planning/phases/03-user-correction-loop/03-PATTERNS.md
@.planning/phases/02-thread-context/02-05-SUMMARY.md

<interfaces>
<!-- Key signatures from src/classifier.js. Extracted from codebase. -->

From src/classifier.js — module header:
  const { db, getConfig } = require('./db');
  const llm = require('./llm');
  const { CATEGORIES, URGENCIES } = require('./llm/providers/base');
  const { fetchThreadContext, buildThreadContext } = require('./llm/thread');

  let broadcast = () => {};
  function setBroadcast(fn) { broadcast = fn; }

From src/classifier.js — classifyEmail() signature:
  async function classifyEmail(userId, emailId) { ... }

From src/classifier.js — Tier 1 short-circuit pattern (lines ~179-194):
  const rulesCategory = rulesClassify(email);
  if (rulesCategory) {
    const { urgency, urgency_reason } = rulesUrgency(rulesCategory, email);
    const extracted = rulesExtractedData(rulesCategory, email);
    const sub = email.subject || '';
    const summary = `${email.from_name || email.from_address} sent: ${sub.substring(0, 80)}${sub.length > 80 ? '...' : ''}.`;
    attempts.delete(attemptKey);
    storeClassification(userId, emailId, {
      category: rulesCategory, urgency, urgency_reason, summary,
      extracted_data: extracted, suggested_tone: 'professional', draft_reply: null,
      source: 'rules'
    }, email);
    return;
  }

From src/classifier.js — storeClassification() signature:
  function storeClassification(userId, emailId, result, email) { ... }
  // result shape: { category, urgency, urgency_reason, summary, extracted_data, suggested_tone, draft_reply, source }

From src/classifier.js — email row fetch (early in classifyEmail):
  const email = db.prepare('SELECT * FROM emails WHERE id = ? AND user_id = ?').get(emailId, userId);

From src/classifier.js — module.exports:
  module.exports = { classifyEmail, classifyAllUnclassifiedForUser, queueClassification, setBroadcast };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert Tier 0 sender-rule lookup into classifyEmail()</name>
  <files>src/classifier.js</files>
  <read_first>
    - src/classifier.js (read in full — locate classifyEmail() body, find the email row fetch,
      find the Tier 1 rulesClassify block, and find the exact insertion point immediately before
      the Tier 1 block; confirm the email variable has from_address available from the SELECT *)
    - .planning/phases/03-user-correction-loop/03-PATTERNS.md (classifier.js section — exact
      Tier 0 block code, module.exports line)
    - .planning/phases/03-user-correction-loop/03-RESEARCH.md (Pitfall 4: sender rule lookup must
      pass userId; Pitfall 3: COUNT query must scope to all three predicates — relevant context
      even though the COUNT is in api.js; understanding why user_id matters here)
  </read_first>
  <behavior>
    - When classifyEmail(userId, emailId) is called and a sender_rules row exists for
      (userId, 'example.com'), storeClassification is called with source='rule' and the function returns
    - When no sender_rules row exists, execution falls through to the existing Tier 1 rulesClassify block
    - The lookup query uses WHERE user_id = ? AND domain = ? — both bound, in that order
    - Domain is extracted as (email.from_address || '').split('@')[1] — the part after '@', no '@' stored
    - When from_address has no '@' character, split('@')[1] is undefined — the Tier 0 block is skipped safely
    - storeClassification is called with source: 'rule' (not 'rules' — matches D-06 specification)
  </behavior>
  <action>
    Read src/classifier.js in full. Find the location inside classifyEmail() where the email row is
    fetched from the DB. The Tier 0 block must be inserted AFTER the email row is fetched (because
    we need email.from_address) and BEFORE the Tier 1 rulesClassify call.

    Insert this block at that location (per D-06 and PATTERNS.md classifier.js section):

      // Tier 0: sender-rule override (D-06, CORRECT-05) — fires before regex, short-circuits immediately
      const domain = (email.from_address || '').split('@')[1];
      if (domain) {
        const senderRule = db.prepare(
          'SELECT category FROM sender_rules WHERE user_id = ? AND domain = ?'
        ).get(userId, domain);
        if (senderRule) {
          const { urgency, urgency_reason } = rulesUrgency(senderRule.category, email);
          const sub = email.subject || '';
          const summary = `${email.from_name || email.from_address} sent: ${sub.substring(0, 80)}${sub.length > 80 ? '...' : ''}.`;
          attempts.delete(attemptKey);
          storeClassification(userId, emailId, {
            category: senderRule.category, urgency, urgency_reason, summary,
            extracted_data: {}, suggested_tone: 'professional', draft_reply: null,
            source: 'rule'
          }, email);
          return;
        }
      }

    Critical: source value is 'rule' (singular) — not 'rules'. This distinguishes Tier 0 sender
    rule hits from Tier 1 regex rule hits (source: 'rules'). Do not change the Tier 1 block's
    source value.

    Do NOT change module.exports — setBroadcast is already exported.
    Do NOT change the Tier 1 block or any other part of classifyEmail().
    Do NOT add any new require() statements — db is already required at the top.
  </action>
  <verify>
    <automated>node --test tests/correction.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/classifier.js contains the literal string 'SELECT category FROM sender_rules WHERE user_id = ? AND domain = ?'
    - src/classifier.js contains "source: 'rule'" (distinct from existing "source: 'rules'" in the Tier 1 block)
    - The Tier 0 block appears before the rulesClassify() call — verify by reading the file and checking line order
    - `node --test tests/correction.test.js` exits 0 — the 'Tier 0 lookup returns null when no sender rule exists' test passes
    - `node --test tests/**/*.test.js` exits 0 with 122+ tests passing (no regressions in classifier tests)
    - `node -e "require('./src/classifier')"` exits 0 with no crash
  </acceptance_criteria>
  <done>
    Tier 0 sender-rule lookup inserted in classifyEmail(). Source value is 'rule'. All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classifyEmail() → sender_rules DB | Tier 0 query must be scoped to user_id to prevent cross-user rule leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Tier 0 sender_rules lookup | mitigate | Query is 'WHERE user_id = ? AND domain = ?' — both params bound; cross-user rule leakage is structurally impossible (V4 ASVS L1) |
| T-03-08 | Spoofing | domain extraction from from_address | accept | Domain is extracted from stored email row (not from request body); the email row was already validated when stored via imap.js; risk is low |
| T-03-09 | Denial of Service | synchronous DB call in classify hot path | accept | better-sqlite3 .get() is synchronous and fast (<1ms on indexed lookup); idx_sender_rules_uq covers the query; acceptable performance profile |
</threat_model>

<verification>
`node --test tests/correction.test.js` — Tier 0 lookup test passes
`node --test tests/**/*.test.js` — full suite 122+ pass, 0 fail
`node -e "require('./src/classifier')"` — exits 0
</verification>

<success_criteria>
- Tier 0 sender-rule lookup inserted before Tier 1 regex block in classifyEmail()
- Query scoped to (user_id, domain) — V4 cross-user access control satisfied
- source value 'rule' distinguishes Tier 0 hits from Tier 1 hits
- No regressions in full test suite
</success_criteria>

<output>
After completion, create .planning/phases/03-user-correction-loop/03-03-SUMMARY.md
</output>
