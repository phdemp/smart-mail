---
phase: 03-user-correction-loop
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/correction.test.js
autonomous: true
requirements:
  - CORRECT-01
  - CORRECT-02
  - CORRECT-05
  - CORRECT-06
  - CORRECT-07

must_haves:
  truths:
    - "tests/correction.test.js exists and all stubs run green (skipped or passing)"
    - "Schema assertion confirms user_corrected_category and corrected_at columns exist after db.js loads"
    - "Sender rule promotion test asserts sender_rules row created after 2 same-domain corrections"
    - "ai_feedback UPSERT test asserts a second vote replaces rather than duplicates the first"
  artifacts:
    - path: "tests/correction.test.js"
      provides: "Test contract for all server-side Phase 3 requirements"
      min_lines: 80
  key_links:
    - from: "tests/correction.test.js"
      to: "src/db.js"
      via: "require('../src/db') after process.env.DB_PATH isolation"
      pattern: "require\\(\\.\\./src/db"
---

<objective>
Create the Wave 0 test file `tests/correction.test.js` that establishes the test contract for all
server-side Phase 3 requirements before any implementation is written.

Purpose: Tests must exist before implementation so that Wave 1 and Wave 2 changes are verified
incrementally with a sub-10-second feedback loop. This is the Nyquist gate — no plan in Wave 1 or
Wave 2 ships without this file green.

Output: `tests/correction.test.js` — isolated test DB, schema assertions, sender rule promotion
tests, ai_feedback UPSERT tests, and /reclassify + /feedback endpoint integration stubs.
</objective>

<execution_context>
@C:/Users/Sudeep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sudeep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-user-correction-loop/03-CONTEXT.md
@.planning/phases/03-user-correction-loop/03-VALIDATION.md
@.planning/phases/03-user-correction-loop/03-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from analog test files. -->

From tests/classifier_validation.test.js — DB isolation pattern:
  process.env.DB_PATH = path.join(__dirname, '..', 'intellimail-correction-test.db');
  test.before(() => { try { fs.rmSync(dbPath, { force: true }); } catch {} });
  test.after(() => { try { fs.rmSync(dbPath, { force: true }); } catch {} });

From tests/migration.test.js — schema assertion pattern:
  const cols = db.prepare('PRAGMA table_info(classifications)').all().map(c => c.name);
  assert.ok(cols.includes('column_name'), 'column_name column must exist');

From tests/api/scoping.test.js — HTTP request helper signature:
  function request(application, method, url, token, body) { ... }
  // Returns Promise<{ status: number, body: string }>

From src/db.js — module export:
  module.exports = { db, getConfig };

From src/classifier.js — module export:
  module.exports = { classifyEmail, classifyAllUnclassifiedForUser, queueClassification, setBroadcast };

From node:test API:
  const test = require('node:test');
  const assert = require('node:assert/strict');
  test('name', () => { ... });        // sync test
  test('name', async () => { ... });  // async test
  test.before(async () => { ... });   // setup hook
  test.after(() => { ... });          // teardown hook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/correction.test.js with schema and DB-layer tests</name>
  <files>tests/correction.test.js</files>
  <read_first>
    - tests/classifier_validation.test.js (DB isolation pattern, test.before/after hooks, better-sqlite3 sync assertions)
    - tests/migration.test.js (PRAGMA table_info schema assertion pattern)
    - src/db.js (module export shape, table schemas, migration block position — confirms what columns will exist after loading)
    - src/classifier.js (classifyEmail signature, setBroadcast export, Tier 1 short-circuit pattern — Tier 0 will mirror it)
  </read_first>
  <action>
    Create tests/correction.test.js using CommonJS require() — no ESM.

    File header: require node:test, node:assert/strict, node:path, node:fs.
    Set process.env.DB_PATH to path.join(__dirname, '..', 'intellimail-correction-test.db') BEFORE
    any require of src/db or src/classifier — the DB_PATH must be set before the module is first
    required so better-sqlite3 opens the isolated file.

    DB isolation:
    - test.before hook 1: delete the test DB file (fs.rmSync with force:true, wrapped in try/catch).
    - test.before hook 2 (runs after hook 1 implicitly — use a single before that does both, or rely
      on ordering): require ../src/db to trigger schema creation, then seed one user row:
        db.prepare('INSERT INTO users (email, password_hash) VALUES (?, ?)').run('correction@test.com', 'hash');
        store userId as global.__corrUserId.
    - test.after hook: delete the test DB file.

    Tests to write (in this order):

    1. 'classifications table has user_corrected_category column (CORRECT-01)'
       - PRAGMA table_info(classifications), assert cols includes 'user_corrected_category'

    2. 'classifications table has corrected_at column (CORRECT-01)'
       - PRAGMA table_info(classifications), assert cols includes 'corrected_at'

    3. 'sender_rules table exists with correct columns (D-04)'
       - PRAGMA table_info(sender_rules), assert cols includes each of: id, user_id, domain, category, created_at

    4. 'ai_feedback table exists with correct columns (D-11)'
       - PRAGMA table_info(ai_feedback), assert cols includes each of: id, summary_id, user_id, vote, created_at

    5. 'sender_rules INSERT OR REPLACE does not duplicate on same unique tuple (CORRECT-05)'
       - Insert two sender_rules rows for same (user_id, domain, category) using INSERT OR REPLACE
       - Assert COUNT(*) from sender_rules WHERE user_id=? AND domain=? AND category=? equals 1

    6. 'sender_rules unique index prevents duplicate (user_id, domain, category) tuples (CORRECT-05)'
       - Insert one row; attempt plain INSERT (not OR REPLACE) for same unique tuple; assert it throws

    7. 'ai_feedback UPSERT replaces vote rather than duplicating (CORRECT-07)'
       - Insert a classifications row to get a valid id as summary_id
       - INSERT OR REPLACE ai_feedback (user_id, summary_id, vote) with vote='up'
       - INSERT OR REPLACE ai_feedback (user_id, summary_id, vote) with vote='down' (same user_id + summary_id)
       - Assert SELECT COUNT(*) = 1 and vote = 'down'

    8. 'ai_feedback vote CHECK constraint rejects invalid values (D-11)'
       - Attempt to INSERT ai_feedback with vote='sideways'; assert it throws

    9. 'sender rule promotion: after 2 corrections same domain+category, sender_rules row exists (CORRECT-05)'
       - Seed: insert 2 emails with from_address 'alice@promo-domain.com', get their ids
       - Insert 2 classifications rows with source='user', category='financial', user_id=corrUserId,
         email_id=each email id
       - Run the COUNT query used by the /reclassify endpoint:
           SELECT COUNT(*) as cnt FROM classifications c
           JOIN emails e ON e.id = c.email_id AND e.user_id = c.user_id
           WHERE c.user_id = ? AND c.source = 'user'
             AND substr(e.from_address, instr(e.from_address, '@') + 1) = ?
             AND c.category = ?
         with params [corrUserId, 'promo-domain.com', 'financial']
       - Assert cnt >= 2
       - Run the UPSERT: INSERT OR REPLACE INTO sender_rules (user_id, domain, category) VALUES (?, ?, ?)
       - Assert SELECT category FROM sender_rules WHERE user_id=? AND domain=? equals 'financial'

    10. 'Tier 0 lookup returns null when no sender rule exists (CORRECT-05)'
        - Query: SELECT category FROM sender_rules WHERE user_id=? AND domain=?
          with params [corrUserId, 'unknown-domain.com']
        - Assert result is undefined (better-sqlite3 .get() returns undefined when no row)

    Each test must use better-sqlite3 synchronous API only (.get(), .run(), .all(), .prepare()).
    No async/await in DB calls. No external HTTP server in this plan — the endpoint integration
    tests with an HTTP helper are deferred to Plan 04 (Wave 2) where the endpoints are implemented.
  </action>
  <verify>
    <automated>node --test tests/correction.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/correction.test.js exists and contains at least 10 named test() blocks
    - `node --test tests/correction.test.js` exits 0 with all tests passing (not skipped, not failed)
    - PRAGMA table_info assertions run against the live schema produced by src/db.js — they validate
      that the migration guards in db.js actually create the columns/tables when run fresh
    - Sender rule promotion test (test 9) verifies the exact COUNT query shape used in the
      /reclassify implementation — the query includes all three WHERE predicates:
      c.user_id = ?, substr(e.from_address, ...) = ?, c.category = ?
    - ai_feedback UPSERT test (test 7) asserts COUNT(*) = 1 after two votes — proving idempotency
    - `node --test tests/**/*.test.js` exits 0 with 122+ tests passing (no regressions)
    - File uses CommonJS require() throughout — no import/export statements
  </acceptance_criteria>
  <done>
    tests/correction.test.js exists, all 10+ tests pass, full test suite remains green at 122+ tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → isolated SQLite DB | Test DB created at process.env.DB_PATH; isolated from production DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | test DB isolation | mitigate | process.env.DB_PATH set before any require; teardown deletes test DB — production DB never touched |
| T-03-02 | Information Disclosure | test seed data | accept | Test DB uses fake emails/users with no real PII; deleted after run |
</threat_model>

<verification>
`node --test tests/correction.test.js` — all tests pass, 0 failures
`node --test tests/**/*.test.js` — full suite 122+ pass, 0 fail
</verification>

<success_criteria>
- tests/correction.test.js created with 10+ passing tests covering CORRECT-01, CORRECT-05, CORRECT-07 schema/DB behavior
- All tests use isolated test DB via process.env.DB_PATH — no production DB risk
- Full test suite remains green
</success_criteria>

<output>
After completion, create .planning/phases/03-user-correction-loop/03-01-SUMMARY.md
</output>
