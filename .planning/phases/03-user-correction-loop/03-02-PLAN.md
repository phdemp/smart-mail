---
phase: 03-user-correction-loop
plan: "02"
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - src/db.js
autonomous: true
requirements:
  - CORRECT-01
  - CORRECT-05
  - CORRECT-07

must_haves:
  truths:
    - "classifications table gains user_corrected_category TEXT and corrected_at DATETIME columns"
    - "sender_rules table exists with unique index on (user_id, domain, category)"
    - "ai_feedback table exists with vote CHECK constraint and unique index on (user_id, summary_id)"
    - "All migrations are idempotent — running db.js twice does not error"
  artifacts:
    - path: "src/db.js"
      provides: "Schema foundation for all Phase 3 correction storage"
      contains: "ALTER TABLE classifications ADD COLUMN user_corrected_category"
  key_links:
    - from: "src/db.js"
      to: "tests/correction.test.js"
      via: "require('../src/db') in test setup"
      pattern: "sender_rules|ai_feedback|user_corrected_category"
---

<objective>
Extend `src/db.js` with all Phase 3 schema additions: two `ALTER TABLE` guards on `classifications`,
and two new table creation guards (`sender_rules`, `ai_feedback`).

Purpose: This is the data foundation. Wave 2 endpoint work and Wave 1 classifier work both depend on
these tables existing. All migrations use the established inline try/catch guard pattern — idempotent
on re-runs, safe on production databases that already have partial migrations.

Output: `src/db.js` with four new migration blocks producing the Phase 3 schema.
</objective>

<execution_context>
@C:/Users/Sudeep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sudeep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-user-correction-loop/03-CONTEXT.md
@.planning/phases/03-user-correction-loop/03-PATTERNS.md
@.planning/phases/02-thread-context/02-CONTEXT.md

<interfaces>
<!-- Existing db.js migration block positions. Extracted from codebase. -->

From src/db.js — existing ALTER TABLE guard pattern (line ~93):
  try { db.exec('ALTER TABLE emails ADD COLUMN is_deleted INTEGER DEFAULT 0'); } catch(e) {}

From src/db.js — existing classifications ALTER TABLE guards (lines ~224-226):
  try { db.exec('ALTER TABLE classifications ADD COLUMN source TEXT'); } catch(e) {}
  try { db.exec('ALTER TABLE classifications ADD COLUMN low_confidence INTEGER DEFAULT 0'); } catch(e) {}

From src/db.js — Phase 2 CREATE TABLE guard (lines ~181-194):
  try {
    db.exec('CREATE TABLE llm_logs (id INTEGER PRIMARY KEY AUTOINCREMENT, ts TEXT NOT NULL, ...)');
  } catch(e) {}

From src/db.js — module export:
  module.exports = { db, getConfig };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 3 schema migrations to src/db.js</name>
  <files>src/db.js</files>
  <read_first>
    - src/db.js (read in full — locate the existing classifications ALTER TABLE block at ~line 224,
      the llm_logs CREATE TABLE block at ~line 181, and the module.exports at the bottom; determine
      exact insertion point for Phase 3 migration blocks)
    - .planning/phases/03-user-correction-loop/03-PATTERNS.md (Pattern 1, Pattern 2, Pattern 3 —
      exact SQL DDL for all four migration blocks including unique index statements)
    - .planning/phases/02-thread-context/02-CONTEXT.md (D-10 — inline guard pattern is locked)
  </read_first>
  <behavior>
    - After migrations run, PRAGMA table_info(classifications) includes user_corrected_category and corrected_at
    - After migrations run, PRAGMA table_info(sender_rules) returns 5 columns: id, user_id, domain, category, created_at
    - After migrations run, PRAGMA table_info(ai_feedback) returns 5 columns: id, summary_id, user_id, vote, created_at
    - Running db.js a second time (re-requiring after first run) does not throw any error
    - INSERT OR REPLACE into sender_rules with same (user_id, domain, category) keeps exactly 1 row
    - INSERT into ai_feedback with vote='invalid' throws (SQLite CHECK constraint)
  </behavior>
  <action>
    Read src/db.js in full to find the correct insertion point. The Phase 3 migration blocks must
    be placed AFTER the last existing classifications ALTER TABLE guard (currently the low_confidence
    and source columns, at approximately line 226).

    Insert four migration blocks in this exact order after the existing classifications guards:

    Block A — classifications audit columns (CORRECT-01, per D-01):
      try { db.exec('ALTER TABLE classifications ADD COLUMN user_corrected_category TEXT'); } catch(e) {}
      try { db.exec('ALTER TABLE classifications ADD COLUMN corrected_at DATETIME'); } catch(e) {}

    Block B — sender_rules table (D-04):
      try {
        db.exec('CREATE TABLE sender_rules (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, domain TEXT, category TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP)');
        db.exec('CREATE UNIQUE INDEX IF NOT EXISTS idx_sender_rules_uq ON sender_rules(user_id, domain, category)');
      } catch(e) {}

    Block C — ai_feedback table (D-11):
      try {
        db.exec('CREATE TABLE ai_feedback (id INTEGER PRIMARY KEY AUTOINCREMENT, summary_id INTEGER REFERENCES classifications(id), user_id INTEGER, vote TEXT CHECK(vote IN (\'up\',\'down\')), created_at DATETIME DEFAULT CURRENT_TIMESTAMP)');
        db.exec('CREATE UNIQUE INDEX IF NOT EXISTS idx_ai_feedback_uq ON ai_feedback(user_id, summary_id)');
      } catch(e) {}

    Each try block must use a single db.exec() call per DDL statement. The CREATE UNIQUE INDEX
    statements go inside the same try block as their table's CREATE TABLE — if the table creation
    is caught (table already exists), the index creation is also skipped, preventing index-on-
    missing-table errors on partial re-runs.

    Do NOT add any pruning/cleanup job for sender_rules or ai_feedback — no retention policy for
    these tables (correction history must be permanent; feedback is a permanent signal).

    Do NOT change module.exports or any other part of db.js.
  </action>
  <verify>
    <automated>node --test tests/correction.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/db.js contains the literal string 'ALTER TABLE classifications ADD COLUMN user_corrected_category TEXT'
    - src/db.js contains the literal string 'ALTER TABLE classifications ADD COLUMN corrected_at DATETIME'
    - src/db.js contains the literal string 'CREATE TABLE sender_rules'
    - src/db.js contains the literal string 'CREATE TABLE ai_feedback'
    - src/db.js contains the literal string "CHECK(vote IN ('up','down'))" (SQLite CHECK constraint)
    - src/db.js contains 'idx_sender_rules_uq' and 'idx_ai_feedback_uq'
    - `node --test tests/correction.test.js` exits 0 — all 10+ schema tests pass
    - `node --test tests/**/*.test.js` exits 0 with 122+ tests passing
    - `node -e "require('./src/db')"` exits 0 with no error output (idempotency check: second load)
  </acceptance_criteria>
  <done>
    src/db.js updated with all four migration blocks. Schema tests green. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db.js startup → SQLite file | Migration guards run at process startup; malformed DDL could crash startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | sender_rules table | mitigate | unique index on (user_id, domain, category) prevents duplicate rules; V4 user_id scoping enforced at query level in classifier.js (Wave 1) |
| T-03-04 | Tampering | ai_feedback table | mitigate | vote TEXT CHECK(vote IN ('up','down')) constraint is defense-in-depth; application-level validation added in Wave 2 endpoint |
| T-03-05 | Denial of Service | migration block at startup | accept | try/catch guard prevents startup crash; SQLite DDL is fast (~1ms); acceptable risk |
| T-03-06 | Information Disclosure | user_corrected_category stored in classifications | mitigate | column is scoped to user via existing user_id on classifications; all queries already include AND user_id = ? |
</threat_model>

<verification>
`node --test tests/correction.test.js` — all schema tests pass
`node --test tests/**/*.test.js` — full suite 122+ pass, 0 fail
`node -e "require('./src/db')"` — exits 0 (idempotent re-run)
</verification>

<success_criteria>
- All four migration blocks present in src/db.js using the inline try/catch guard pattern
- Schema tests from Plan 01 pass against the new migrations
- No regressions in the full test suite
</success_criteria>

<output>
After completion, create .planning/phases/03-user-correction-loop/03-02-SUMMARY.md
</output>
