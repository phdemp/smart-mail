---
phase: 02-thread-context
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/llm/thread.test.js
  - tests/llm/base.test.js
  - tests/llm/router.test.js
autonomous: true
requirements:
  - THREAD-01
  - THREAD-02
  - THREAD-03
  - THREAD-04
  - THREAD-05
  - THREAD-06
  - THREAD-07

must_haves:
  truths:
    - "Running node --test tests/llm/thread.test.js exits non-zero (tests exist but imports fail — expected at Wave 0)"
    - "tests/llm/base.test.js contains tests named 'buildPrompt injects ## Prior thread context when opts.threadContext present' and 'buildDraftPrompt injects ## Prior thread context when opts.threadContext present'"
    - "tests/llm/router.test.js contains a test named 'classify inserts row into llm_logs on success' and 'classify inserts row into llm_logs on error'"
    - "tests/llm/thread.test.js contains tests for stripQuotedReplies, buildThreadContext, and fetchThreadContext covering all THREAD-01 through THREAD-05 behaviors"
  artifacts:
    - path: "tests/llm/thread.test.js"
      provides: "Thread utility unit + integration tests (new file)"
      min_lines: 80
    - path: "tests/llm/base.test.js"
      provides: "Extended with opts.threadContext injection tests"
    - path: "tests/llm/router.test.js"
      provides: "Extended with llm_logs INSERT tests"
  key_links:
    - from: "tests/llm/thread.test.js"
      to: "src/llm/thread.js"
      via: "require('../../src/llm/thread')"
      pattern: "require.*src/llm/thread"
---

<objective>
Create the test scaffolding for all Phase 2 behaviors before any implementation lands. This is the Nyquist gate: every THREAD requirement has a corresponding failing test that the implementation plans will make pass.

Purpose: Ensure no requirement ships without a test. Wave 0 tests will fail on import (src/llm/thread.js does not yet exist) — that is the expected RED state.
Output: tests/llm/thread.test.js (new), tests/llm/base.test.js (extended), tests/llm/router.test.js (extended).
</objective>

<execution_context>
@C:\Users\Sudeep\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Sudeep\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-thread-context/02-CONTEXT.md
@.planning/phases/02-thread-context/02-RESEARCH.md
@.planning/phases/02-thread-context/02-PATTERNS.md

<interfaces>
From tests/llm/base.test.js (existing file — extend, do not replace):
- Uses node:test and node:assert/strict
- Requires ../../src/llm/providers/base
- Existing tests: buildPrompt body truncation, parseProviderResponse clean JSON, markdown fence, trailing chatter

From tests/llm/router.test.js (existing file — extend, do not replace):
- Uses node:test, node:assert/strict
- Sets process.env.DB_PATH before requiring src modules (DB isolation pattern)
- Tests classify() and generateDraft() with mocked providers

From tests/classifier_validation.test.js (DB isolation pattern source):
- Sets unique DB_PATH before any require of src/db or src/classifier
- test.before seeds users table: db.prepare('INSERT INTO users (email) VALUES (?)').run(...)
- test.after cleans up: fs.rmSync(dbPath, { force: true })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/llm/thread.test.js with full test suite for thread utilities</name>
  <files>tests/llm/thread.test.js</files>
  <read_first>
    tests/classifier_validation.test.js
    tests/llm/base.test.js
    .planning/phases/02-thread-context/02-PATTERNS.md
  </read_first>
  <behavior>
    - stripQuotedReplies('Hello\n> quoted line\nWorld') returns 'Hello\nWorld'
    - stripQuotedReplies with line matching /^On .{10,80} wrote:$/i removes that line
    - stripQuotedReplies with line matching /^-{3,} ?original message/i removes that line
    - stripQuotedReplies returns original text when stripped result is fewer than 100 chars AND original was longer (D-08)
    - buildThreadContext([]) returns null
    - buildThreadContext(null) returns null
    - buildThreadContext(7 messages) returns string with at most 5 [From: blocks (oldest 2 dropped, per D-05/D-06)
    - buildThreadContext(5 messages each with 600-char body) returns string with length <= 6000 (D-06 budget cap)
    - buildThreadContext formats each message as [From: name <addr>]\n[Subject: subj]\nbody_snippet
    - fetchThreadContext(userId, email with raw_headers null) uses subject-normalized fallback and returns array of emails with same normalized subject (D-02)
    - fetchThreadContext(userId, email) never returns emails from other users (V4 cross-user isolation)
    - fetchThreadContext(userId, email with raw_headers containing in-reply-to) uses header path and returns matching emails (D-01)
    - fetchThreadContext(userId, email) excludes the triggering email itself from results
  </behavior>
  <action>
    Create tests/llm/thread.test.js. Follow the DB isolation pattern from tests/classifier_validation.test.js exactly: set process.env.DB_PATH to a unique path (e.g., path.join(__dirname, '..', '..', 'intellimail-thread-test.db')) BEFORE any require of src/db or src/llm/thread. Use test.before to require modules and seed a test user. Use test.after to rmSync the DB file.

    File header must use: const test = require('node:test'); const assert = require('node:assert/strict'); const path = require('node:path'); const fs = require('node:fs');

    Structure tests in two groups:
    1. Pure-function tests (no DB needed): stripQuotedReplies (3 tests), buildThreadContext (4 tests). These run without DB setup.
    2. DB integration tests (require DB): fetchThreadContext (3 tests). Seed the emails table with test rows in test.before or within each test using db.prepare().

    For the fetchThreadContext cross-user isolation test: create two users, seed emails for each with identical normalized subject, call fetchThreadContext with userId of user 1, assert that no results have user_id of user 2.

    For the header-based test: insert an email with raw_headers = JSON.stringify({'in-reply-to': '<prior-msg-id@test>', 'references': ''}) and message_id = 'prior-msg-id@test' for userId; call fetchThreadContext with the triggering email that has raw_headers containing that in-reply-to value.

    All tests import from '../../src/llm/thread' — this module does not exist yet, so the file will fail on require. That is the expected RED state for Wave 0. Add a comment: // Wave 0: src/llm/thread.js not yet created — tests will fail on import until Plan 03 lands.

    Do NOT use async/await on the DB calls in tests (better-sqlite3 is synchronous). Do use async test callbacks when calling fetchThreadContext or buildThreadContext if they eventually become async — but per D-01/D-02, they are synchronous.
  </action>
  <verify>
    <automated>node --test "tests/llm/thread.test.js" 2>&1 | head -5</automated>
    The command should show a module-not-found error for src/llm/thread — confirming the test file itself is syntactically valid but the implementation is missing (RED state). If you see a syntax error instead, fix the syntax before proceeding.
  </verify>
  <done>
    tests/llm/thread.test.js exists with at least 10 test() calls covering stripQuotedReplies (3), buildThreadContext (4), and fetchThreadContext (3+). Running the file produces "Cannot find module '../../src/llm/thread'" or equivalent import error, not a syntax error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/llm/base.test.js and tests/llm/router.test.js with Phase 2 failing stubs</name>
  <files>tests/llm/base.test.js, tests/llm/router.test.js</files>
  <read_first>
    tests/llm/base.test.js
    tests/llm/router.test.js
  </read_first>
  <behavior>
    base.test.js additions:
    - 'buildPrompt injects ## Prior thread context when opts.threadContext present': buildPrompt(email, { threadContext: 'some context' }) returns string containing both '## Prior thread context' and '## Current email'
    - 'buildPrompt without opts.threadContext is unchanged': buildPrompt(email, {}) does NOT contain '## Prior thread context'
    - 'buildDraftPrompt injects ## Prior thread context when opts.threadContext present': buildDraftPrompt(email, { threadContext: 'ctx', tone: 'friendly' }) returns string containing '## Prior thread context' and '## Current email'
    - 'buildDraftPrompt without opts.threadContext is unchanged': buildDraftPrompt(email, {}) does NOT contain '## Prior thread context'

    router.test.js additions:
    - 'classify inserts row into llm_logs on success': after classify() resolves successfully, llm_logs table contains a row with outcome='success', provider matching the mock provider name, and token_count > 0
    - 'classify inserts row into llm_logs on provider error': after classify() rejects, llm_logs table contains a row with outcome != 'success' and latency_ms >= 0
  </behavior>
  <action>
    Append new test() blocks to tests/llm/base.test.js after the existing tests. The new tests call buildPrompt and buildDraftPrompt (already imported at the top of the file) with an opts object containing threadContext: 'THREAD CTX'. Assert using assert.match(out, /## Prior thread context/) and assert.match(out, /## Current email/). For the negative cases, assert.ok(!out.includes('## Prior thread context')).

    Append new test() blocks to tests/llm/router.test.js. These tests require access to the db after classify() resolves. The existing router.test.js may need process.env.DB_PATH set to an isolated path before requiring src/llm/router (check if it already does). If it does not, add DB_PATH isolation before the require block. After classify() is called with a mock provider that resolves successfully, query the llm_logs table: db.prepare('SELECT * FROM llm_logs WHERE outcome = ?').get('success'). Assert the row exists with non-null token_count.

    The base.test.js additions will FAIL (assert fails) because buildPrompt does not yet inject the section headers — RED state is expected. The router.test.js additions may fail because llm_logs table does not exist and/or the router does not INSERT — RED state expected.

    Add a comment above each new block: // Phase 2 THREAD-0X: fails until Plan 04 (base.js) / Plan 05 (router.js) lands.
  </action>
  <verify>
    <automated>node --test "tests/llm/base.test.js" 2>&1 | tail -10</automated>
    Existing tests should still pass. The two new base.test.js tests should fail with assertion errors (not syntax errors). The router.test.js new tests should fail with either table-not-found or assertion errors.
  </verify>
  <done>
    tests/llm/base.test.js has 4 new test() calls for opts.threadContext. tests/llm/router.test.js has 2 new test() calls for llm_logs. All pre-existing tests in both files still pass. New tests fail with assertion errors (RED state confirmed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → src modules | Test files require production modules; no untrusted input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | tests/llm/thread.test.js DB isolation | mitigate | Each test file sets a unique DB_PATH before requiring src/db — prevents cross-test DB state leakage |
| T-02-02 | Tampering | Test DB teardown | accept | test.after removes the isolated DB file; stale test DBs do not affect production intellimail.db |
</threat_model>

<verification>
1. node --test "tests/llm/thread.test.js" — outputs import error for src/llm/thread (not a syntax error)
2. node --test "tests/llm/base.test.js" — existing tests pass, 4 new tests fail with AssertionError
3. node --test "tests/llm/router.test.js" — existing tests pass, 2 new tests fail
4. node --test "tests/**/*.test.js" — 103 pre-existing tests still pass (new failing tests are additive)
</verification>

<success_criteria>
- tests/llm/thread.test.js created with 10+ test cases covering all THREAD-01 through THREAD-05 behaviors
- tests/llm/base.test.js extended with 4 new THREAD-04/THREAD-07 tests (all failing as expected)
- tests/llm/router.test.js extended with 2 new THREAD-06 tests (all failing as expected)
- Full test suite (node --test "tests/**/*.test.js") still reports 103 pre-existing tests passing
- No syntax errors in any modified test file
</success_criteria>

<output>
After completion, create .planning/phases/02-thread-context/02-01-SUMMARY.md
</output>
