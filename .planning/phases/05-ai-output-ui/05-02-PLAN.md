---
phase: 05-ai-output-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/api.js
  - src/llm/templates.js
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "Every email list row shows a tier badge (Rule/AI/AI?/Failed) after the category badge"
    - "Travel, financial, and meeting_request list rows show a key-fact 3rd line before the badge row"
    - "Other categories (fyi, legal, pitch_deck, rewards_awards, other) show no 3rd line"
    - "Tier badge correctly maps: rule/rules=Rule(green), failed=Failed(red), llm/fallback=AI(blue)"
    - "When low_confidence=1 on llm source, badge shows 'AI ?' with amber ? suffix"
    - "template reply fallback works for 'warm' tone (does not return undefined)"
  artifacts:
    - path: "src/routes/api.js"
      provides: "tierBadge() helper, keyFactLine() helper, SQL SELECT patch, list row template update"
      contains: "function tierBadge"
    - path: "src/llm/templates.js"
      provides: "warm tone entries in GENERIC and TABLE"
      contains: "warm:"
  key_links:
    - from: "GET /api/emails SQL SELECT"
      to: "email.source, email.low_confidence"
      via: "c.source, c.low_confidence added to SELECT"
      pattern: "c\\.source, c\\.low_confidence"
    - from: "list row map() callback"
      to: "tierBadge(email.source, email.low_confidence)"
      via: "helper call in template literal"
      pattern: "tierBadge\\(email\\.source"
    - from: "list row map() callback"
      to: "keyFactLine(cat, email.extracted_data)"
      via: "helper call in template literal"
      pattern: "keyFactLine\\(cat"
---

<objective>
Add the tier attribution badge and extracted key-fact line to the email list view.

Purpose: List-view is the primary surface for progressive disclosure (UI-07). Every email must show its classification source (Rule/AI/Failed) and the 3 relevant categories must expose their key structured data fact without requiring the email to be opened.
Output: tierBadge() and keyFactLine() helpers in api.js, SQL SELECT expanded with c.source + c.low_confidence, list row template updated, templates.js expanded with warm tone entries.
</objective>

<execution_context>
@C:\Users\Sudeep\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Sudeep\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\projects\NeuralInbox\.planning\PROJECT.md
@C:\projects\NeuralInbox\.planning\ROADMAP.md
@C:\projects\NeuralInbox\.planning\phases\05-ai-output-ui\05-CONTEXT.md
@C:\projects\NeuralInbox\.planning\phases\05-ai-output-ui\05-UI-SPEC.md
@C:\projects\NeuralInbox\.planning\phases\05-ai-output-ui\05-PATTERNS.md

<interfaces>
<!-- From src/routes/api.js — exact current state of relevant sections -->

<!-- escHtml() — lines 21–29, always use for user-sourced data in template literals -->
<!-- parsedExtractedData(str) — lines 85–87: try{return JSON.parse(str||'{}')}catch{return{}} -->
<!-- categoryLabel(cat) — lines 76–83: maps category keys to display names -->

<!-- GET /api/emails SQL SELECT — line 352–359 (MUST PATCH): -->
<!-- CURRENT: SELECT e.*, c.category, c.urgency, c.urgency_reason, c.summary, c.extracted_data, c.suggested_tone -->
<!-- REQUIRED: add c.source, c.low_confidence at end of SELECT column list -->

<!-- Email list row map() callback — lines 386–418: -->
<!-- Line 387: const cat = email.category || 'pending'; -->
<!-- Line 388: const urg = email.urgency || 'normal'; -->
<!-- Line 391: const preview = (email.summary || email.body_text || '').substring(0,120)... -->
<!-- Line 408 (preview div): <div style="font-size:12px;...margin-bottom:6px;font-family:'Literata'...">${escHtml(preview)}</div> -->
<!-- Line 409 (badge div): <div style="display:flex;align-items:center;gap:6px;flex-wrap:wrap;"> -->
<!-- Line 410 (category badge): <span class="badge badge-${escHtml(cat)}">${escHtml(categoryLabel(cat))}</span> -->

<!-- Helper section end — line 87 (parsedExtractedData), helpers section ends before line 89 account routes comment -->

<!-- From src/llm/templates.js — exact current structure: -->
<!-- GENERIC has keys: formal, professional, friendly, brief -->
<!-- TABLE has category keys: meeting_request, financial, legal, travel, pitch_deck, fyi, rewards_awards, other -->
<!-- Each category has keys: formal, professional, friendly, brief -->
<!-- buildTemplateReply(classification, tone) at line 54 — returns table[tone] || table.professional -->
<!-- 'warm' key is MISSING everywhere — must be added to avoid undefined returns -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add tierBadge() and keyFactLine() helpers, patch SQL SELECT</name>
  <files>src/routes/api.js</files>
  <read_first>
    - src/routes/api.js (lines 85–90 for helper insertion point; lines 352–360 for SQL SELECT; lines 386–418 for list row template)
    - .planning/phases/05-ai-output-ui/05-PATTERNS.md (Pattern Assignments — api.js list row section; helper function patterns; tierBadge and keyFactLine code excerpts)
    - .planning/phases/05-ai-output-ui/05-UI-SPEC.md (Component 1: Tier Attribution Badge — source logic; Component 3: Extracted Key-Fact Line — field selection logic; Copywriting Contract)
    - .planning/phases/05-ai-output-ui/05-CONTEXT.md (D-02 source mapping; D-07 key-fact field selection; D-08 category exclusions)
  </read_first>
  <behavior>
    - tierBadge('rule', 0) returns HTML with class badge-tier-rule and text "Rule"
    - tierBadge('rules', 0) returns HTML with class badge-tier-rule (rules normalized to Rule, per D-02)
    - tierBadge('llm', 0) returns HTML with class badge-tier-ai and text "AI"
    - tierBadge('fallback', 0) returns HTML with class badge-tier-ai and text "AI" (per D-02)
    - tierBadge('llm', 1) returns HTML with badge-tier-ai badge-tier-ai--uncertain and tier-lc-suffix span containing "?"
    - tierBadge('failed', 0) returns HTML with class badge-tier-failed and text "Failed"
    - tierBadge(null, 0) returns AI badge (null/unknown treated as AI per D-02)
    - keyFactLine('travel', JSON with departure_date and flight_number) returns div.email-key-fact "Flight · BA456 · Dec 3"
    - keyFactLine('travel', JSON with only pnr) returns div.email-key-fact "PNR · ABC123"
    - keyFactLine('travel', JSON without departure_date and without pnr) returns empty string
    - keyFactLine('financial', JSON with amount_due and due_date) returns div.email-key-fact "Due · $450 · Apr 30"
    - keyFactLine('financial', JSON with only amount_due) returns div.email-key-fact "Due · $450"
    - keyFactLine('meeting_request', JSON with meeting_date and meeting_time) returns div.email-key-fact "Meeting · Thu · 3pm"
    - keyFactLine('fyi', any JSON) returns empty string (per D-08)
    - keyFactLine('legal', any JSON) returns empty string (per D-08)
    - keyFactLine('other', any JSON) returns empty string (per D-08)
  </behavior>
  <action>
    THREE edits to src/routes/api.js:

    EDIT 1 — SQL SELECT patch (line 353):
    Change the SELECT column list from:
      SELECT e.*, c.category, c.urgency, c.urgency_reason, c.summary, c.extracted_data, c.suggested_tone
    To:
      SELECT e.*, c.category, c.urgency, c.urgency_reason, c.summary, c.extracted_data, c.suggested_tone, c.source, c.low_confidence
    Nothing else changes in the query. This is the critical fix that makes email.source and email.low_confidence available in the list row map() callback.

    EDIT 2 — Add two helper functions after parsedExtractedData() (after line 87, before the account routes comment at line 89):

    function tierBadge(source, lowConfidence):
    - Normalize source to lowercase string, default to empty string on null/undefined
    - src === 'rule' or src === 'rules' → cls='badge-tier-rule', label='Rule'
    - src === 'failed' → cls='badge-tier-failed', label='Failed'
    - all other values (llm, fallback, empty, null) → cls='badge-tier-ai'
      - if lowConfidence is truthy: label = 'AI <span class="tier-lc-suffix">?</span>', append ' badge-tier-ai--uncertain' to cls
      - else: label = 'AI'
    - return: '<span class="badge ' + cls + '"><span class="tier-dot"></span>' + label + '</span>'
    Use escHtml() for no user-controlled data passes through this function (source comes from DB classifications.source column), but still follow the pattern.

    function keyFactLine(cat, extractedDataStr):
    - Call parsedExtractedData(extractedDataStr) to get object d
    - 'travel': if d.departure_date exists → format "Flight · {flight_number} · {departure_date}" 
      (omit flight_number segment if empty, avoid double-dot: replace ' · ·' with ' ·')
      elif d.pnr exists → "PNR · {pnr}"
      else → empty string
    - 'financial': d.amount_due exists → if d.due_date also → "Due · {amount_due} · {due_date}" else "Due · {amount_due}"
      else → empty string
    - 'meeting_request': d.meeting_date exists → if d.meeting_time also → "Meeting · {meeting_date} · {meeting_time}" else "Meeting · {meeting_date}"
      else → empty string
    - all other categories → empty string (per D-08)
    - When fact is non-empty: wrap with escHtml() on each data value and return '<div class="email-key-fact">' + fact + '</div>'
    - When fact is empty: return '' (no div rendered)
    Copy-exact label format from UI-SPEC.md Copywriting Contract table.

    EDIT 3 — Update list row map() callback template literal:
    After "const preview = ..." line (~line 391), add two new const lines:
      const tierBadgeHtml = tierBadge(email.source, email.low_confidence);
      const keyFact = keyFactLine(cat, email.extracted_data);

    Inside the template literal:
    a) Insert ${keyFact} between the preview div and the badge div (between line 408 and 409).
       The preview div ends with ...margin-bottom:6px;">...</div> — insert keyFact immediately after that closing </div>.
    b) Inside the badge div, after the category badge span:
       <span class="badge badge-${escHtml(cat)}">${escHtml(categoryLabel(cat))}</span>
       Insert ${tierBadgeHtml} immediately after that span.

    PROHIBITED: Do not change the gap value on the badge div or any other list row layout. Do not add escHtml() wrapping around tierBadgeHtml (tierBadge() already returns a safe HTML string, double-escaping would corrupt the markup). Do not change the SQL WHERE clause or JOIN.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('src/routes/api.js', 'utf8');
const checks = [
  'function tierBadge(',
  'function keyFactLine(',
  'c.source, c.low_confidence',
  'tierBadge(email.source, email.low_confidence)',
  'keyFactLine(cat, email.extracted_data)',
  '\${keyFact}',
  '\${tierBadgeHtml}'
];
const missing = checks.filter(c => !src.includes(c));
if (missing.length) { console.error('MISSING in api.js:', missing); process.exit(1); }
console.log('All list-row changes present');
"</automated>
  </verify>
  <done>
    - SQL SELECT now includes c.source, c.low_confidence
    - tierBadge() function is present after parsedExtractedData() in the helpers section
    - keyFactLine() function is present after tierBadge()
    - List row map() callback declares tierBadgeHtml and keyFact constants
    - ${keyFact} rendered between preview div and badge div
    - ${tierBadgeHtml} rendered immediately after the category badge span inside the badge div
  </done>
</task>

<task type="tdd">
  <name>Task 2: Add 'warm' tone entries to templates.js</name>
  <files>src/llm/templates.js</files>
  <read_first>
    - src/llm/templates.js (full file — 62 lines)
    - .planning/phases/05-ai-output-ui/05-CONTEXT.md (D-11: tone labels Brief/Formal/Warm)
  </read_first>
  <behavior>
    - buildTemplateReply({ category: 'other' }, 'warm') returns a non-undefined string
    - buildTemplateReply({ category: 'meeting_request' }, 'warm') returns a non-undefined string
    - buildTemplateReply({ category: 'financial' }, 'warm') returns a non-undefined string
    - buildTemplateReply({ category: 'travel' }, 'warm') returns a non-undefined string
    - buildTemplateReply({ category: 'legal' }, 'warm') returns a non-undefined string (legal always returns professional regardless of tone)
    - buildTemplateReply({ category: 'fyi' }, 'warm') returns a non-undefined string
    - buildTemplateReply({ category: 'rewards_awards' }, 'warm') returns a non-undefined string
    - buildTemplateReply({ category: 'pitch_deck' }, 'warm') returns a non-undefined string
    - Existing 'formal', 'professional', 'friendly', 'brief' entries are unchanged
  </behavior>
  <action>
    Add 'warm' key to GENERIC and to every category in TABLE.

    The 'warm' tone is friendly but professional — slightly warmer than 'professional', slightly more measured than 'friendly'. Use the existing 'friendly' value as the model for the appropriate register.

    GENERIC.warm: 'Thanks for reaching out — I'll look into this and get back to you.'
    TABLE.meeting_request.warm: 'Thanks for the invite — I'll check my calendar and confirm shortly.'
    TABLE.financial.warm: 'Thanks for the notice — I'll review the details and follow up.'
    TABLE.legal.warm: value is irrelevant because buildTemplateReply returns table.professional for legal regardless — but add 'warm' entry for completeness: same as professional value.
    TABLE.travel.warm: 'Thanks for the booking details — I'll review and reach out if anything needs adjusting.'
    TABLE.pitch_deck.warm: 'Thanks for sharing this — I'll take a look and get back to you.'
    TABLE.fyi.warm: 'Thanks for the heads-up — noted!'
    TABLE.rewards_awards.warm: 'Thanks for letting me know — I'll review before the deadline.'
    TABLE.other.warm: same as GENERIC.warm (TABLE.other === GENERIC, so adding to GENERIC covers it)

    Confirm module.exports still exports buildTemplateReply, TABLE, GENERIC unchanged.
  </action>
  <verify>
    <automated>node -e "
const { buildTemplateReply, GENERIC, TABLE } = require('./src/llm/templates');
const cats = ['other','meeting_request','financial','legal','travel','pitch_deck','fyi','rewards_awards'];
let errors = [];
cats.forEach(cat => {
  const result = buildTemplateReply({ category: cat }, 'warm');
  if (!result || typeof result !== 'string') errors.push(cat + ': returned ' + result);
});
if (!GENERIC.warm) errors.push('GENERIC missing warm key');
if (errors.length) { console.error('FAILURES:', errors); process.exit(1); }
console.log('All warm tone entries valid');
"</automated>
  </verify>
  <done>
    - GENERIC.warm entry exists and is a non-empty string
    - Every category in TABLE has a warm entry
    - buildTemplateReply({category: X}, 'warm') returns a defined string for all 8 categories
    - All existing tone keys (formal, professional, friendly, brief) are unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → template literal | classifications.source and low_confidence values flow from DB into HTML template strings |
| DB → keyFactLine | extracted_data JSON from DB is parsed and its string values are embedded in HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Injection (XSS) | keyFactLine() — extracted_data field values in HTML | mitigate | All extracted data fields (d.departure_date, d.flight_number, d.pnr, d.amount_due, d.due_date, d.meeting_date, d.meeting_time) MUST be wrapped in escHtml() before embedding in the returned div string. Pattern: `escHtml(d.departure_date || '')`. |
| T-05-02-02 | Injection (XSS) | tierBadge() — source column value | accept | source column is enum-controlled by classifier (values: rule, rules, llm, fallback, failed). Normalized to lowercase internally. Label strings are static ('Rule', 'AI', 'Failed') — never user input. No escHtml needed on label; dot span contains no user data. |
| T-05-02-03 | Information Disclosure | c.low_confidence in SELECT | accept | low_confidence is a boolean (0/1) from classifications table, scoped by user_id via existing WHERE clause. No cross-user leakage risk. |
</threat_model>

<verification>
After both tasks complete:

1. Helpers present and SQL patched:
node -e "const fs=require('fs');const src=fs.readFileSync('src/routes/api.js','utf8');['function tierBadge(','function keyFactLine(','c.source, c.low_confidence','tierBadge(email.source','keyFactLine(cat, email.extracted_data)'].forEach(c=>{if(!src.includes(c)){console.error('MISSING:',c);process.exit(1);}});console.log('OK');"

2. Warm tone entries:
node -e "const {buildTemplateReply}=require('./src/llm/templates');['other','meeting_request','financial','legal','travel','pitch_deck','fyi','rewards_awards'].forEach(cat=>{const r=buildTemplateReply({category:cat},'warm');if(!r)throw new Error(cat+' warm undefined');});console.log('warm entries OK');"

3. Test suite:
node --test "tests/**/*.test.js"
(141 tests; 0 failures expected)
</verification>

<success_criteria>
- GET /api/emails SQL now selects c.source and c.low_confidence
- tierBadge() helper correctly maps all source values per D-02
- keyFactLine() returns key-fact div for travel/financial/meeting_request, empty string for all other categories
- Both helpers use escHtml() on all user-sourced string values
- List row template renders ${keyFact} before badge row and ${tierBadgeHtml} after category badge
- templates.js warm tone entries exist for all 8 categories
- node --test passes with 141 tests
</success_criteria>

<output>
After completion, create .planning/phases/05-ai-output-ui/05-02-SUMMARY.md
</output>
