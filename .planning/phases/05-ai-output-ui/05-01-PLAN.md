---
phase: 05-ai-output-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/app.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07

must_haves:
  truths:
    - "Tier badge dots render with neomorphic convex shadow (not flat)"
    - "Tone chips display with convex raised shadow in default state, pressed shadow when selected"
    - "Attribution footer text appears in IBM Plex Mono 11px var(--text-muted)"
    - "Extracted key-fact line renders in IBM Plex Mono 11px with ellipsis overflow"
    - "FAILED badge has convex-sm shadow — never flat"
    - "Draft section has blue-tinted background distinct from surrounding content"
    - "No hardcoded hex values in any new CSS — all references via CSS variables"
  artifacts:
    - path: "public/css/app.css"
      provides: "Neomorphic shadow scale vars + all Phase 5 component classes"
      contains: "--shadow-convex-sm"
  key_links:
    - from: "public/css/app.css :root"
      to: ".badge-tier-rule, .badge-tier-ai, .badge-tier-failed"
      via: "var(--shadow-convex-sm)"
      pattern: "shadow-convex-sm"
    - from: "public/css/app.css :root"
      to: ".tone-chip"
      via: "var(--shadow-convex-sm), var(--shadow-hover), var(--shadow-pressed)"
      pattern: "tone-chip"
---

<objective>
Add the neomorphic CSS foundation and all Phase 5 component classes to app.css.

Purpose: Every new UI element introduced in Phase 5 (tier badges, tone chips, attribution footer, key-fact line, draft section) needs its CSS present before the HTML is rendered. CSS is the only deliverable of this plan — no JS or server changes.
Output: New shadow scale custom properties in :root, new class blocks for tier badges, tone picker, skip link, attribution footer, key-fact line, and draft section.
</objective>

<execution_context>
@C:\Users\Sudeep\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Sudeep\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\projects\NeuralInbox\.planning\PROJECT.md
@C:\projects\NeuralInbox\.planning\ROADMAP.md
@C:\projects\NeuralInbox\.planning\phases\05-ai-output-ui\05-UI-SPEC.md
@C:\projects\NeuralInbox\.planning\phases\05-ai-output-ui\05-PATTERNS.md

<interfaces>
<!-- From public/css/app.css — existing :root variables in use -->
<!-- :root block is lines 7–29. Dark theme at lines 31–43. -->
<!-- Existing variables confirmed present: -->
<!--   --bg-base: #f0f2f7 -->
<!--   --bg-panel: #ffffff -->
<!--   --bg-raised: #e8ecf5   ← tone-chip background, already declared -->
<!--   --bg-hover: #dde3f5 -->
<!--   --border: #c8d0e5 -->
<!--   --text-primary: #1a2545 -->
<!--   --text-secondary: #4a5578 -->
<!--   --text-muted: #7a86a8 -->
<!--   --accent-blue: #3b82f6 -->
<!--   --accent-green: #10b981 -->
<!--   --accent-red: #ef4444 -->
<!--   --accent-amber: #f59e0b -->

<!-- Existing CSS blocks and their line ranges (for insert-point accuracy): -->
<!--   .badge block: lines 291–303 -->
<!--   Category badge classes (.badge-meeting_request etc): lines 304–312 -->
<!--   .badge-pending + @keyframes pulse-pending: lines 314–324 -->
<!--   /* ── Draft Editor ── */ comment: line 326 -->
<!--   .draft-editor textarea styles: lines 327–345 -->
<!--   @keyframes urgency-pulse: lines 271–274 -->
<!--   @keyframes toast-in / toast-out: lines 424–... -->

<!-- Insert point for all Phase 5 CSS: -->
<!--   AFTER @keyframes pulse-pending block (after line 324) -->
<!--   BEFORE /* ── Draft Editor ── */ comment (line 326) -->
<!-- This keeps Phase 5 additions grouped before the draft editor block -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add neomorphic shadow variables to :root and insert all Phase 5 CSS classes</name>
  <files>public/css/app.css</files>
  <read_first>
    - public/css/app.css (lines 1–50 for :root block; lines 270–350 for badge/draft insertion point)
    - .planning/phases/05-ai-output-ui/05-UI-SPEC.md (Design Tokens Summary section; all Component CSS blocks)
    - .planning/phases/05-ai-output-ui/05-PATTERNS.md (shared CSS variable usage section)
  </read_first>
  <action>
    Two edits to public/css/app.css:

    EDIT 1 — Append to :root block (before the closing brace of :root, after --sidebar-width: 240px):
    Add the five shadow custom properties and four border-radius tokens:
    --shadow-convex-sm, --shadow-convex, --shadow-hover, --shadow-pressed, --shadow-concave-sm,
    --radius-sm (8px), --radius-md (14px), --radius-lg (20px), --radius-pill (9999px).
    Exact values are in UI-SPEC.md "Design Tokens Summary" section.
    Do NOT touch any existing variable. Insert only after the last existing variable (--sidebar-width).

    EDIT 2 — Insert Phase 5 class blocks after the @keyframes pulse-pending block and before the /* ── Draft Editor ── */ comment.
    Add in this order, each with a /* ── Phase 5: [Name] ── */ section comment:

    a) Tier badge base and variants — .tier-dot, .badge-tier-rule, .badge-tier-ai,
       .badge-tier-ai--uncertain, .tier-lc-suffix, .badge-tier-failed.
       Key constraints (all from UI-SPEC.md):
       - .tier-dot: 8px width+height, border-radius 50%, display inline-block, margin-right 4px, box-shadow var(--shadow-convex-sm)
       - .badge-tier-rule background: rgba(16,185,129,0.12) color: var(--accent-green) box-shadow: var(--shadow-convex-sm)
       - .badge-tier-ai background: rgba(59,130,246,0.12) color: var(--accent-blue) box-shadow: var(--shadow-convex-sm)
       - .badge-tier-ai--uncertain background: rgba(245,158,11,0.08) — overrides the ai background
       - .tier-lc-suffix color: var(--accent-amber) margin-left: 4px
       - .badge-tier-failed background: rgba(239,68,68,0.12) color: var(--accent-red) box-shadow: var(--shadow-convex-sm) [MUST have box-shadow — convex constraint from sketch-findings]
       - Dot colors: .badge-tier-rule .tier-dot { background: var(--accent-green); } etc.
       - NO border property on any tier badge — shadow defines edges (anti-pattern from UI-SPEC)

    b) Extracted key-fact line — .email-key-fact:
       font-family 'IBM Plex Mono' monospace, font-size 11px, color var(--text-muted),
       white-space nowrap, overflow hidden, text-overflow ellipsis, margin-bottom 8px, line-height 1.3

    c) Draft section container — .draft-section:
       background rgba(59,130,246,0.06), border-radius 12px, padding 16px, margin-top 16px

    d) Draft attribution label — .draft-attribution-label:
       font-family 'IBM Plex Mono' monospace, font-size 10px, color var(--accent-blue),
       text-transform uppercase, letter-spacing 0.08em, margin-bottom 12px, font-weight 600

    e) Tone picker container — .tone-picker:
       display flex, align-items center, gap 8px, margin-bottom 12px, flex-wrap wrap

    f) Tone picker label — .tone-picker-label:
       font-family 'IBM Plex Mono' monospace, font-size 10px, color var(--text-muted),
       text-transform uppercase, letter-spacing 0.06em, margin-right 4px

    g) Tone chip button — .tone-chip and states:
       default: font-family 'Syne' sans-serif, font-size 13px, font-weight 600,
                padding 8px 12px, border-radius 9999px, border none,
                background var(--bg-raised), color var(--text-secondary),
                cursor pointer, box-shadow var(--shadow-convex-sm),
                transition box-shadow 120ms, color 120ms
       :hover — box-shadow var(--shadow-hover), color var(--text-primary)
       :active — box-shadow var(--shadow-pressed)
       .tone-chip--selected — box-shadow var(--shadow-pressed), color var(--accent-blue),
                              border 1.5px solid var(--accent-blue)

    h) Draft skip link — .draft-skip-link:
       background none, border none, font-family 'Syne' sans-serif, font-size 13px,
       color var(--text-muted), cursor pointer, padding 0,
       text-decoration underline, text-underline-offset 4px, transition color 150ms
       :hover — color var(--text-secondary)

    i) Attribution footer — .attribution-footer:
       font-family 'IBM Plex Mono' monospace, font-size 11px, color var(--text-muted),
       text-align right, padding 8px 0, margin-top 16px,
       border-top 1px solid var(--border), letter-spacing 0.03em

    j) Draft slide-in animation:
       @keyframes draft-slide-in { from { opacity:0; transform:translateY(-8px); } to { opacity:1; transform:translateY(0); } }
       .draft-section[x-cloak] { display:none !important; }

    PROHIBITED: no hardcoded hex values — every color must reference a CSS variable or be a rgba() using the known hex values from :root. No 12px font-size. No font-weight 500. No non-4-point px values (forbidden: 2px, 3px, 6px, 7px, 14px in new declarations). No border on tier badges except .tone-chip--selected.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('public/css/app.css','utf8');const checks=['--shadow-convex-sm','--shadow-hover','--shadow-pressed','--shadow-concave-sm','--radius-pill','.tier-dot','.badge-tier-rule','.badge-tier-ai','.badge-tier-failed','.badge-tier-ai--uncertain','.tier-lc-suffix','.email-key-fact','.draft-section','.draft-attribution-label','.tone-picker','.tone-picker-label','.tone-chip','.tone-chip--selected','.draft-skip-link','.attribution-footer','draft-slide-in'];const missing=checks.filter(c=>!css.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('All Phase 5 CSS tokens present');"</automated>
  </verify>
  <done>
    - public/css/app.css contains all 5 shadow variables and 4 radius tokens in :root
    - All 10 new class blocks exist in the file
    - No hardcoded hex colors in any new declaration
    - No non-4-point px values in new declarations
    - .badge-tier-failed has box-shadow: var(--shadow-convex-sm) (convex constraint satisfied)
    - .tone-chip--selected has border: 1.5px solid var(--accent-blue)
    - Existing CSS is unchanged (no existing variables or class blocks removed)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File write | CSS file edited by executor — only static declarations, no user data flows through CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-CSS-01 | Tampering | public/css/app.css | accept | CSS is a static asset; no user-controlled data enters CSS. Risk is developer error (hardcoded hex, wrong values) — caught by automated verify check. |
| T-05-CSS-02 | Information Disclosure | CSS variable naming | accept | Shadow variables expose design constants, not sensitive data. Acceptable for a client-side design system. |
</threat_model>

<verification>
After Task 1 completes:
node -e "const fs=require('fs');const css=fs.readFileSync('public/css/app.css','utf8');const checks=['--shadow-convex-sm','--shadow-hover','--shadow-pressed','--shadow-concave-sm','--radius-pill','.tier-dot','.badge-tier-rule','.badge-tier-ai','.badge-tier-failed','.badge-tier-ai--uncertain','.tier-lc-suffix','.email-key-fact','.draft-section','.draft-attribution-label','.tone-picker','.tone-picker-label','.tone-chip','.tone-chip--selected','.draft-skip-link','.attribution-footer','draft-slide-in'];const missing=checks.filter(c=>!css.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('All Phase 5 CSS tokens present');"

Test suite must remain green:
node --test "tests/**/*.test.js"
(141 tests; 0 failures expected — CSS changes do not affect backend tests)
</verification>

<success_criteria>
- public/css/app.css contains all 5 neomorphic shadow variables added to :root
- All 10 Phase 5 class blocks are present and follow the anti-pattern constraints from UI-SPEC.md
- .badge-tier-failed has box-shadow: var(--shadow-convex-sm) (not flat — sketch-findings constraint)
- .tone-chip--selected has 1.5px border + pressed shadow + accent-blue color
- No existing CSS rules were removed or modified
- node --test passes with 141 tests
</success_criteria>

<output>
After completion, create .planning/phases/05-ai-output-ui/05-01-SUMMARY.md
</output>
