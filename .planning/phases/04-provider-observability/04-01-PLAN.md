---
phase: 04-provider-observability
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/api/health.test.js
  - tests/server.test.js
autonomous: true
requirements:
  - OBSERVE-01
  - OBSERVE-04
  - OBSERVE-05

must_haves:
  truths:
    - "tests/api/health.test.js exists and contains failing stubs for the health endpoint shape and pill fragment behavior"
    - "tests/server.test.js exists and contains a failing stub for the 30-second warmup delay"
    - "npm test still passes 132 tests green after stubs are added (stubs skip or are pending — they must not pass yet)"
  artifacts:
    - path: "tests/api/health.test.js"
      provides: "Stub tests for OBSERVE-01 and OBSERVE-04"
      contains: "GET /api/llm/health, GET /api/llm/health/pill"
    - path: "tests/server.test.js"
      provides: "Stub test for OBSERVE-05 warmup delay"
      contains: "classifyAllUnclassifiedForUser"
  key_links:
    - from: "tests/api/health.test.js"
      to: "src/routes/api.js"
      via: "Express test app mounting the health routes"
      pattern: "api/llm/health"
    - from: "tests/server.test.js"
      to: "src/server.js"
      via: "mock timers / dependency injection for init()"
      pattern: "classifyAllUnclassifiedForUser"
---

<objective>
Create failing test stubs for Phase 4's new backend behaviors before any implementation touches source files.

Purpose: Nyquist compliance — every automated test target must exist before the wave that implements it. Wave 1 cannot begin until these stubs are green (tests pass or skip). The stubs define the behavioral contract implementation must satisfy.

Output: Two new test files with stubs that describe the expected behavior for the health endpoint (OBSERVE-01), pill fragment (OBSERVE-04), and startup warmup delay (OBSERVE-05).
</objective>

<execution_context>
@C:/Users/Sudeep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sudeep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projects/NeuralInbox/.planning/ROADMAP.md
@C:/projects/NeuralInbox/.planning/phases/04-provider-observability/04-CONTEXT.md
@C:/projects/NeuralInbox/.planning/phases/04-provider-observability/04-RESEARCH.md
@C:/projects/NeuralInbox/.planning/phases/04-provider-observability/04-VALIDATION.md
@C:/projects/NeuralInbox/.planning/phases/04-provider-observability/04-PATTERNS.md

<interfaces>
From tests/api/scoping.test.js (HTTP test helper — copy verbatim):
- process.env.JWT_SECRET_PATH and process.env.DB_PATH set before any require
- express app constructed inline, routes mounted with requireAuth
- request(application, method, url, token) — returns Promise<{ status, body }>
- db.prepare('INSERT INTO users (email) VALUES (?)').run(...).lastInsertRowid for seeding
- const { signToken } = require('../../src/auth') for JWT tokens
- test.after cleanup with fs.rmSync(process.env.DB_PATH)

From tests/llm/router.test.js (DB isolation pattern):
- const dbPath = path.join(__dirname, '..', '..', 'intellimail-router-test.db')
- process.env.DB_PATH = dbPath (set before requiring src/db or src/llm/router)
- test.before / test.after with fs.rmSync for isolation

From src/llm/router.js (getProviderHealth shape):
- getProviderHealth(userId) returns { nvidia: { status, last_error, last_error_at, last_error_msg, last_success_at }, groq: {...}, gemini: {...}, deepseek: {...} }
- status values: 'ok' | 'rate_limited' | 'service_busy' | 'invalid_key' | 'breaker_open' | 'unknown'

From src/server.js (init structure):
- init() calls classifyAllUnclassifiedForUser(u.id) and startSyncForUser(u.id) for each user
- After Phase 4: classifyAllUnclassifiedForUser wrapped in setTimeout(..., 30000)
- startSyncForUser remains immediate (not in setTimeout)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/api/health.test.js with stubs for OBSERVE-01 and OBSERVE-04</name>
  <files>tests/api/health.test.js</files>

  <read_first>
    - tests/api/scoping.test.js — copy the exact file setup, request() helper, seed() pattern, and test.after cleanup; do not reinvent
    - tests/llm/router.test.js lines 1-13 — DB isolation env var pattern (set DB_PATH before require)
    - .planning/phases/04-provider-observability/04-PATTERNS.md section "tests/api/health.test.js" — lists the 4 tests to implement and the mock injection strategy
  </read_first>

  <action>
    Create tests/api/health.test.js as a new file. Use a unique DB path: intellimail-health-test.db. Set process.env.JWT_SECRET_PATH to data-test-health/jwt.secret and process.env.DB_PATH before any require.

    Copy the request() helper from scoping.test.js verbatim (it is the project standard HTTP test helper). Copy the seed() pattern to create one test user and one email row.

    The Express test app for health.test.js must mount the two routes inline rather than importing all of api.js. The routes need a mock for llm.router.getProviderHealth — inject it via a module-level variable that the test controls. Pattern: define getProviderHealth as a variable pointing to a function, mount a route that calls the variable, swap the variable between tests.

    Write these 4 tests in stub/todo form (use test.todo() so they are skipped but counted):

    1. "GET /api/llm/health returns 401 without token" — asserts status 401
    2. "GET /api/llm/health returns { providers, failed_count } shape with valid token" — asserts res.body.providers is an object and res.body.failed_count is a number
    3. "GET /api/llm/health/pill returns empty string when all providers ok or unknown" — asserts response body is empty string
    4. "GET /api/llm/health/pill returns amber pill HTML when any provider non-ok" — asserts response body contains the exact text "AI features degraded" and does NOT contain any provider name ("nvidia", "groq", "gemini", "deepseek") or the words "circuit breaker", "rate limited"

    Use test.todo() for all 4 so they are pending (not passing, not failing). The full suite must still exit 0.

    Add test.after cleanup to remove the DB file.
  </action>

  <verify>
    <automated>node --test "tests/api/health.test.js" 2>&1; node --test "tests/**/*.test.js" 2>&1 | grep -E "pass|fail"</automated>
  </verify>

  <acceptance_criteria>
    - tests/api/health.test.js exists
    - Running node --test "tests/api/health.test.js" exits 0 (todos are not failures)
    - The file contains the string "GET /api/llm/health returns 401" (test name present)
    - The file contains the string "AI features degraded" (pill language assertion present)
    - npm test (full suite) still reports ℹ pass 132, ℹ fail 0 (existing tests unaffected)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tests/server.test.js with stub for OBSERVE-05 warmup delay</name>
  <files>tests/server.test.js</files>

  <read_first>
    - src/server.js lines 85-105 — the current init() function; understand what classifyAllUnclassifiedForUser and startSyncForUser are and where they are called
    - .planning/phases/04-provider-observability/04-PATTERNS.md section "tests/server.test.js" — Node.js 24 mock.timers pattern and the dependency-injection approach for testing setTimeout
    - tests/llm/router.test.js lines 1-11 — node:test framework setup (require patterns, no external deps)
  </read_first>

  <action>
    Create tests/server.test.js as a new file. This test does NOT need DB isolation (server.js init() is being tested for timing behavior only, not DB writes).

    The test strategy: do NOT import server.js directly (it starts the HTTP server). Instead, extract the testable behavior into a helper. Write the test to verify that an init-like function with a setTimeout delay behaves correctly:

    Write 1 test in stub form using test.todo():

    1. "classifyAllUnclassifiedForUser is called after 30s delay, startSyncForUser is called immediately" — description of test: call an init function that wraps classifyAllUnclassifiedForUser in setTimeout(fn, 30000) and calls startSyncForUser immediately; use node:test mock.timers.enable(['setTimeout']); assert syncFn was called synchronously; assert classifyFn was NOT called synchronously; advance mock.timers.tick(30000); assert classifyFn was called; call mock.timers.reset()

    Use test.todo() so the stub is pending (not failing). The full suite must still exit 0.

    No external dependencies. Use only node:test (require('node:test')) and node:assert/strict.
  </action>

  <verify>
    <automated>node --test "tests/server.test.js" 2>&1; node --test "tests/**/*.test.js" 2>&1 | grep -E "pass|fail"</automated>
  </verify>

  <acceptance_criteria>
    - tests/server.test.js exists
    - Running node --test "tests/server.test.js" exits 0
    - The file contains the string "classifyAllUnclassifiedForUser" (the function name being tested)
    - The file contains the string "30000" (the delay constant)
    - npm test (full suite) still reports ℹ pass 132, ℹ fail 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Express test app | Test-controlled inputs; no real auth tokens reach a live server — isolated per-test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Information Disclosure | health.test.js mock setup | accept | Test DB is ephemeral (deleted in test.after); no real credentials or provider keys in test fixtures |
| T-04-W0-02 | Tampering | test.todo() stubs | accept | Stubs cannot pass until implementation is correct; no false confidence from pending tests |
</threat_model>

<verification>
After both tasks complete:
- node --test "tests/api/health.test.js" exits 0 with 4 todo tests
- node --test "tests/server.test.js" exits 0 with 1 todo test
- npm test reports ℹ pass 132, ℹ fail 0
</verification>

<success_criteria>
- tests/api/health.test.js exists with 4 named todo stubs covering OBSERVE-01 and OBSERVE-04
- tests/server.test.js exists with 1 named todo stub covering OBSERVE-05
- Full suite remains green at 132 passing, 0 failing
- Both new files follow the existing node:test patterns (no vitest, no jest)
</success_criteria>

<output>
After completion, create .planning/phases/04-provider-observability/04-01-SUMMARY.md
</output>
